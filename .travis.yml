language: java
jdk:
- oraclejdk8
cache:
  directories:
  - "$HOME/.m2"

services:
  - postgresql

before_script:
  - psql -c "CREATE DATABASE planner --locale=uk_UA.UTF-8;" -U postgres
  - psql -c "CREATE USER planner WITH PASSWORD 'postgres';" -U postgres
#  - psql -c 'create database travis_ci_test;' -U postgres planner --password postgres --locale=uk_UA.UTF-8
